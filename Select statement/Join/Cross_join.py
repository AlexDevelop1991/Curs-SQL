"""CROSS JOIN - это перекрестное соединения каждая строка из первой таблицы объединяется с каждой строкой с второй таблицы."""
"""SELECT column(s)
FROM table_1
CROSS JOIN
table_2;"""
# SELECT * FROM countries CROSS JOIN regions;

# SELECT * FROM countries CROSS JOIN regions
# WHERE countries.region_id >= 2
# ORDER BY country_id;
