"""CREATE TABLE schema.table ORGANIZATION HEAP
(column_name datatype DEFAULT expr,
column_name datatype DEFAULT expr,
...);"""

# CREATE TABLE students (
# sudent_id INTEGER,
# name VARCHAR2(15),
# start_date DATE DEFAULT ROUND(SYSDATE),
# scholarship NUMBER(6, 2),
# avg_scoore NUMBER(4, 2) DEFAULT 5
# );

# 